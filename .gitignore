node_modules/
.next/
*.tsbuildinfo
.DS_Store

# Holds the live Leadi5 API key — never commit
triton-humming-valley/config.php
triton-humming-valley/leads-failed.log
# Upload archives are built with config.php inside them, so they carry the
# live API key and must never be committed. Rebuild with:
#   zip -r triton-humming-valley.zip triton-humming-valley
#   tar -czf triton-humming-valley.tar.gz -C triton-humming-valley .
triton-humming-valley.zip
triton-humming-valley.tar.gz

# Generated by build/ scripts — rebuild with:
#   python3 build/placeholders.py && python3 build/make_preview.py
build/art.json
build/preview.html

# Stray screenshots from local verification runs
*.png
triton-humming-valley-update.tar.gz
cover-images.tar.gz
images-update.tar.gz
images.zip
images-folder.tar.gz
