Hero media (all optional — the hero falls back to a gradient if absent):

  hero.mp4          background film, muted loop, keep under ~20 MB
  hero-poster.jpg   first frame, shown while the video loads
  hero-bg.jpg       still image behind the film
