PLACEHOLDER — no real file here yet.

Save the real file in this folder as exactly:
  Brochure.pdf

Then open "Triton Sales Portal.html", find the "up-in-the-clouds"
project's "brochure" folder entry, and remove `missing: true` from
that file so the portal shows it instead of "Not added yet". Delete
this placeholder once the real file is in place.
